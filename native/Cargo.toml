[package]
name = "maditor-native"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = "0.2.2"
maditor-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
gpui = { version = "0.2.2", features = ["test-support"] }
