[package]
name = "madi-gyeol"
version = "0.1.0"
edition = "2021"
description = "Madi's UI on the gyeol toolkit (replaces the GPUI app piece by piece)"

[[bin]]
name = "madi-gyeol"
path = "src/main.rs"

[dependencies]
gyeol = { path = "../../../gyeol" }
madi-project = { path = "../project" }
madi-text = { path = "../text" }
