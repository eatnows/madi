[package]
name = "madi"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = "0.2.2"
madi-git = { path = "../git" }
madi-project = { path = "../project" }
madi-editor = { path = "../editor" }
madi-ui = { path = "../ui" }
chrono = "0.4"

[dev-dependencies]
gpui = { version = "0.2.2", features = ["test-support"] }
