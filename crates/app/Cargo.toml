[package]
name = "maditor"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = "0.2.2"
maditor-git = { path = "../git" }
maditor-project = { path = "../project" }
maditor-text = { path = "../text" }
unicode-segmentation = "1"
chrono = "0.4"

[dev-dependencies]
gpui = { version = "0.2.2", features = ["test-support"] }
