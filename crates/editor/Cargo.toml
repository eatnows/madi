[package]
name = "madi-editor"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = "0.2.2"
madi-text = { path = "../text" }
madi-ui = { path = "../ui" }

[dev-dependencies]
gpui = { version = "0.2.2", features = ["test-support"] }
