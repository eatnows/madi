[package]
name = "madi-project"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
madi-git = { path = "../git" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
