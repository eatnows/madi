[package]
name = "madi-syntax"
version = "0.1.0"
edition = "2021"
description = "Real syntax-tree-based highlighting and symbol extraction for Madi, via bundled tree-sitter grammars (no plugin download, no WASM runtime)"

[dependencies]
streaming-iterator = "0.1"
tree-sitter = "0.24"
tree-sitter-rust = "0.23"
